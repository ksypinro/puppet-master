# Codex skill metadata (agents/openai.yaml).
# Additive: runtimes that do not understand this file ignore it.
# Spec: https://learn.chatgpt.com/docs/build-skills
interface:
  display_name: "iOS Memory Debugger"
  short_description: "Investigate iOS app memory from heap snapshots"
  default_prompt: "Use $ios-memory-debugger to find out what is retaining this object."

policy:
  # Capture pauses the target briefly and writes an artifact; it never edits
  # source, relaunches, or resets data without explicit authorization.
  allow_implicit_invocation: true
